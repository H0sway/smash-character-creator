// Import dependencies
import React from 'react';

// Functional component, no need to use a React component for now.
const Home = () => {
  render() {
    return (
      <div className="Home">

      </div>
    )
  }
}

export default Home;
